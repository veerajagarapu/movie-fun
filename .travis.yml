jobs:
  include:
    - stage: database migration
      language: java
      jdk: openjdk11
      addons:
        mariadb: '10.2'
      install: skip
      before_script:
        - echo 'starting migration'
        - wget -q -O - https://packages.cloudfoundry.org/debian/cli.cloudfoundry.org.key | sudo apt-key add -
        - echo "deb https://packages.cloudfoundry.org/debian stable main" | sudo tee /etc/apt/sources.list.d/cloudfoundry-cli.list
        - sudo apt-get update
        - sudo apt-get install cf-cli
        - cf install-plugin -r "CF-Community" mysql-plugin -f
        - cf login -a https://api.run.pivotal.io -u vj0044789@techmahindra.com -s sandbox -o vj0044789.techmahindra.com -p fcfm8nyk
        - curl https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/5.1.1/flyway-commandline-5.1.1-linux-x64.tar.gz | tar xvz
        - flyway-*/flyway -url="jdbc:mysql://q-n3s3y1.q-g3198.bosh:3306/service_instance_db?user=2aa095b7ca0d4ce3a26ef32fcdcdc827\u0026password=lhyu2u134r8ffi3w\u0026useSSL=false" -locations=filesystem:db -user=2aa095b7ca0d4ce3a26ef32fcdcdc827 -password=lhyu2u134r8ffi3w clean migrate